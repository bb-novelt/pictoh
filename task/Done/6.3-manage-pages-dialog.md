# Task 6.3: Manage Pages Dialog

**Epic**: Epic 6: Edit Mode & Configuration

## Task Details

- [ ] Create `ManagePagesDialog` component
- [ ] Display list of all pages
- [ ] **Sort pages by name alphabetically**
- [ ] Highlight current page
- [ ] Prevent deletion of Home page
- [ ] Add actions per page:
- [ ] Confirm before deletion
- [ ] Handle edge cases (deleting current page)

## File Location

Place all files for this task inside `src/editMode/` (feature folder for edit mode).
Example: `src/editMode/ManagePagesDialog.tsx`

## Completion Summary

**Date Completed**: 2026-02-24

**Work Done**:

- Created `apps/pictho-app/src/editMode/ManagePagesDialog.tsx` using MUI components (`Dialog`, `List`, `ListItem`, `ListItemText`, `TextField`, `IconButton`, `Typography`, etc.)
- Features implemented:
  - Pages are sorted **alphabetically** (French locale collation via `localeCompare`)
  - **Current page** is highlighted with `action.selected` background and bold text
  - Each page row shows three action buttons: **Ouvrir** (navigate), **Renommer** (inline edit), **Supprimer** (delete)
  - **Home page** delete button is disabled (`isHome` check)
  - **Rename** enters inline edit mode: Enter key confirms, Escape cancels; validates non-empty name and no duplicate names (case-insensitive)
  - **Delete** opens a confirmation `Dialog` with the page name before calling `deletePage()`; deleting the current page automatically navigates to Home (handled inside `pageActions.deletePage`)
  - Navigating via "Ouvrir" closes the dialog immediately after calling `navigateToPage()`
- Exported `ManagePagesDialog` from `editMode/index.ts`
- Updated `app/app.tsx` to:
  - Import `ManagePagesDialog`
  - Add `managePagesOpen` boolean state
  - Pass `onManagePages={() => setManagePagesOpen(true)}` to `EditModeToolbar` (prop was already declared but unused)
  - Render `<ManagePagesDialog open={managePagesOpen} onClose={...} />` alongside the other dialogs
