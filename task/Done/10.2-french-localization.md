# Task 10.2: French Localization

**Epic**: Epic 10: Polish & Testing

## Task Details

- [x] Review all UI text in French
- [x] Ensure proper French grammar and spelling
- [x] Use appropriate French typography
- [x] Test with French TTS voice

## Completion Summary

**Date Completed**: 2026-03-03

**Work Done**:

**Comprehensive review of all French UI strings** across every component:

| File                    | Text reviewed                                                 |
| ----------------------- | ------------------------------------------------------------- |
| `ErrorBoundary.tsx`     | Error fallback UI — grammar correct ✓                         |
| `CreatePageDialog.tsx`  | Create-page dialog labels and validation — correct ✓          |
| `ManagePagesDialog.tsx` | Page list dialog — all labels, tooltips, confirmation text ✓  |
| `EditModeToolbar.tsx`   | Edit-mode toolbar buttons — correct ✓                         |
| `SquareEditModal.tsx`   | Square edit modal — all labels and options — correct ✓        |
| `PictureSelector.tsx`   | Picture library browser — search, filters, upload — correct ✓ |
| `FirstLaunchLoader.tsx` | First-launch progress messages — correct ✓                    |
| `OfflineIndicator.tsx`  | "Hors ligne" chip — correct ✓                                 |
| `SaveIndicator.tsx`     | "Sauvegardé" chip — correct ✓                                 |

**French typography fixes applied**:

- `FirstLaunchLoader.tsx`: `'Prêt !'` → `'Prêt\u00a0!'` — non-breaking space before `!` (French typography rule)
- `FirstLaunchLoader.tsx`: `'Erreur du worker : …'` → `'Erreur du worker\u00a0: …'` — non-breaking space before `:` (French typography rule)
- `ManagePagesDialog.tsx`: delete confirmation `» ? Cette action` → `»{'\u00a0'}? Cette action` — non-breaking space before `?` (French typography rule)

**Already correct** (no changes needed):

- Ellipsis character `…` used throughout (not `...`) ✓
- Guillemets `«\u00a0text\u00a0»` already using `&nbsp;` around them ✓
- Grammar and spelling verified correct in all strings ✓

**TTS French voice** (`ttsService.ts`):

- Language set to `fr-FR` ✓
- Prefers French female voices (Amélie, Marie, Siwis pattern) ✓
- Falls back to first available French voice ✓
- Works fully offline via Web Speech API built into Android/iOS tablets ✓

**Notes**:

- All typography changes are non-breaking-space fixes (invisible at the character level but semantically meaningful — they prevent line-break between punctuation and the word before it)
- No i18n framework was needed — the app is French-only by design
