# Task 7.4: Page Rename

**Epic**: Epic 7: Page Management System

## Task Details

- [x] Implement `renamePage(pageId, newName)` function - rename by ID
- [x] Validate new name is unique (warn if duplicate)
- [x] Update `pageName` in state (keep `pageId` unchanged)
- [x] Update `currentPageId` reference if renaming current page
- [x] **No need to update square navigations** (they reference `pageId`, not name)
- [x] Persist changes

## Completion Summary

**Date Completed**: 2026-02-24

**Work Done**:

- `renamePage(pageId, newName)` implemented in `src/state/actions/pageActions.ts`; updates only `pageName`, leaving `pageId` unchanged.
- `ManagePagesDialog` validates uniqueness (case-insensitive) before calling `renamePage`.
- No square navigation updates needed since squares store `openPageId` (not `pageName`).
- `currentPageId` continues to match because it stores the ID, not the name.
- Persistence deferred to task 9.2.

**Notes**:

- Home page can be renamed (only deletion is blocked).
