# Task 9.4: Data Validation

**Epic**: Epic 9: Persistence & Storage

## Task Details

- [ ] Validate loaded data structure
- [ ] Check for required fields
- [ ] Fix or discard invalid data
- [ ] Ensure backwards compatibility
- [ ] Log validation errors

## File Location

Place all files for this task inside `src/storage/` (feature folder for persistence).
Example: `src/storage/dataValidation.ts`

## Completion Summary

- Created `src/storage/dataValidation.ts` exporting `validateAppConfig(raw: unknown): AppConfig | null` which deeply validates all levels (AppConfig, Page, Square, Picture), discards invalid items, sanitises fixable inconsistencies (orphaned `currentPageId` reset to `homePageId`), and logs all errors/warnings.
- Updated `src/storage/rehydrateStore.ts` to use `validateAppConfig` instead of the previous local `isValidConfig` + `sanitizeConfig` functions.
- Created `src/storage/dataValidation.test.ts` with 36 tests covering all validation paths, error logging, sanitisation, and backwards compatibility.
- All 170 tests pass; linter clean; CodeQL: no alerts.
