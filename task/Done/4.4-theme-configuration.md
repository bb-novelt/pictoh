# Task 4.4: Theme Configuration

**Epic**: Epic 4: Main Grid Layout & Basic UI

## Task Details

- [ ] Create custom MUI theme
- [ ] Define French-specific font stack
- [ ] Set color palette (primary, secondary)
- [ ] Configure typography for tablet readability
- [ ] Set default touch target sizes
- [ ] Configure elevation/shadows for cards

## File Location

Place theme configuration inside `src/shared/` (used across the whole app).
Example: `src/shared/theme.ts`

## Completion Summary

**Date Completed**: 2026-02-23

**Work Done**:

- Created `apps/pictho-app/src/shared/theme.ts` with a custom MUI theme:
  - French-friendly font stack: Segoe UI, Roboto, system-ui, -apple-system, sans-serif
  - Color palette: deep blue primary (#1565C0), warm orange secondary (#F57C00), red error for edit-mode borders
  - Typography scaled up (base 16px) for tablet readability; `textTransform: 'none'` on buttons
  - Minimum touch target sizes (44×44px) enforced on MuiButton and MuiIconButton
  - Custom 25-level shadow scale with subtle, tablet-optimised shadows
  - Component overrides for Card, Paper, DialogTitle, and OutlinedInput
- Applied theme in `apps/pictho-app/src/main.tsx` by wrapping the app with `<ThemeProvider>` and adding `<CssBaseline />` for consistent baseline styles

**Notes**:

- `CssBaseline` resets browser default styles (margins, box-sizing, etc.) for a consistent baseline across all tablet browsers
- All component overrides use `styleOverrides` so they can still be individually overridden at component level
