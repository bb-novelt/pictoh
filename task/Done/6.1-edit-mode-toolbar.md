# Task 6.1: Edit Mode Toolbar

**Epic**: Epic 6: Edit Mode & Configuration

## Task Details

- [x] Create `EditModeToolbar` component
- [x] Position at top of screen
- [x] Add "Create New Page" button
- [x] Add "Manage Pages" button
- [x] Add "Exit Edit Mode" button
- [x] Style with Material UI AppBar
- [x] Toolbar only visible in edit mode

## File Location

Place all files for this task inside `src/editMode/` (feature folder for edit mode).
Example: `src/editMode/EditModeToolbar.tsx`

## Completion Summary

**Date Completed**: 2026-02-24

**Work Done**:

- Created `apps/pictho-app/src/editMode/EditModeToolbar.tsx` using MUI `AppBar` + `Toolbar` with `position="static"` so it takes normal document flow space
- Added three French-labeled buttons with MUI icons:
  - "Créer une nouvelle page" (AddIcon) — accepts optional `onCreatePage` prop (for task 6.2)
  - "Gérer les pages" (ListIcon) — accepts optional `onManagePages` prop (for task 6.3)
  - "Quitter le mode édition" (CloseIcon) — calls `disableEditMode()` directly, aligned to the right via `ml: 'auto'`
- Exported `EditModeToolbar` from `editMode/index.ts`
- Updated `app/app.tsx` to:
  - Import `useSnapshot` from valtio and `store` from state
  - Wrap content in a flex-column `Box` (full width/height)
  - Conditionally render `<EditModeToolbar />` only when `snap.isEditMode` is true
  - Wrap `<Grid />` in a `flex: 1; minHeight: 0` container so it correctly fills remaining space below the toolbar

**Notes**:

- `onCreatePage` and `onManagePages` props on `EditModeToolbar` are left optional so tasks 6.2 and 6.3 can wire them up without changing the component interface
- Used `minHeight: 0` on the grid wrapper to prevent flex overflow in some browsers
