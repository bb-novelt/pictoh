/**
 * Text-to-Speech service (placeholder).
 *
 * Task 5.2 requires the interaction layer to call speak() on touch.
 * The full local-model TTS implementation is done in Task 5.3.
 * This stub satisfies the interface so the rest of the app compiles
 * and can be wired up before the real engine is ready.
 */

/**
 * Speak the given text aloud.
 *
 * Currently a no-op placeholder; will be replaced with the local TTS
 * engine in Task 5.3.
 *
 * @param text - Text to synthesise
 */
export function speak(_text: string): void {
  // TODO(5.3): replace with local TTS model call
}
